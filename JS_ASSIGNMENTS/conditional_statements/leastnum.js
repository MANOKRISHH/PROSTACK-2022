//WAP to print least number in a given two numbers

// Initialize the two numbers
let num1 = 5;
let num2 = 10;

// Determine which number is lesser
if (num1 < num2) {
  // If num1 is lesser, print it
  console.log(num1);
} else {
  // If num2 is lesser, print it
  console.log(num2);
}
