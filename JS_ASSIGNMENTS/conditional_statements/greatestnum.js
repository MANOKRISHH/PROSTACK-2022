//WAP to print the greatest number in given two numbers

// Initialize the two numbers
let num1 = 5;
let num2 = 10;

// Determine which number is greater
if (num1 > num2) {
  // If num1 is greater, print it
  console.log(num1);
} else {
  // If num2 is greater, print it
  console.log(num2);
}
