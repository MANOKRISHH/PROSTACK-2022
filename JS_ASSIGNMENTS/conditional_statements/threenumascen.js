//WAP to print the given 3 numbers in ascending order

// Declare an array with the three numbers
const numbers = [5, 2, 7];

// Sort the array in ascending order
numbers.sort((a, b) => a - b);

// Print the sorted array
console.log(numbers); // Output: [2, 5, 7]
